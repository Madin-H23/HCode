# JSONL 是唯一真相源，SQLite 只做索引层

会话数据的唯一真相源保持上游的 JSONL 追加文件（attach/恢复语义绑定其上，崩溃安全语义现成）；SQLite 仅作索引层——会话列表、标题、时间、token 合计、后续全文检索——且必须可随时 drop+rebuild，不承载任何唯一数据。拒绝全面迁移 SQLite：那会把每次拉上游更新都变成存储层冲突，还得重写一套崩溃安全语义。
